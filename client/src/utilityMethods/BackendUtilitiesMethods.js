import axios from 'axios'

export const checkIfUserHasAConversation = async ({ sender, receiver, conversation }) => {
    // make a call to the server and check if current user (sender) is having any conversation with receiver
    
}